# 🚀 התחלה מהירה - שתי אפשרויות

יש לך **שתי דרכים** להריץ את המערכת:

---

## 🌐 אפשרות 1: Google Colab (הכי מהיר - 2 דקות!)

**מושלם לבדיקה מהירה** - לא צריך להתקין כלום במחשב שלך!

### שלבים:

1. **פתח את הקובץ** `Live_Transcription_Colab.ipynb` ב-Google Colab:
   - העלה את הקובץ ל-Google Drive
   - או לחץ על: [Open in Colab] (אם זה ב-GitHub)

2. **הרץ את התא הראשון** - התקנת תלויות (30 שניות)

3. **הזן את ה-API Key** בתא השני:
   ```python
   OPENAI_API_KEY = "sk-YOUR-KEY-HERE"
   ```
   קבל מפתח מכאן: https://platform.openai.com/api-keys

4. **הרץ את שאר התאים** עד לתא האחרון

5. **לחץ על הקישור** `*.gradio.live` שיופיע

6. **סמן "כאן 11"** ולחץ "התחל תמלול"!

### יתרונות:
- ✅ לא צריך להתקין כלום
- ✅ עובד מכל מקום
- ✅ הכל מוכן מראש
- ✅ חינמי!

### חסרונות:
- ⚠️ הסשן נמחק אחרי 12 שעות
- ⚠️ צריך להריץ מחדש בכל פעם

---

## 💻 אפשרות 2: מחשב מקומי (לשימוש ארוך טווח)

**מושלם לשימוש קבוע** - התקנה חד-פעמית

### שלבים:

1. **התקן ffmpeg**:
   ```bash
   # Windows (Chocolatey)
   choco install ffmpeg

   # Mac
   brew install ffmpeg

   # Linux
   sudo apt-get install ffmpeg
   ```

2. **שכפל את הקוד**:
   ```bash
   git clone <repository-url>
   cd live_transciber
   ```

3. **התקן תלויות Python**:
   ```bash
   pip install -r requirements.txt
   ```

4. **הגדר API Key**:
   ```bash
   # ערוך את קובץ .env
   nano .env

   # החלף:
   OPENAI_API_KEY=sk-your-key-here
   ```

5. **בדוק שהכל עובד**:
   ```bash
   python test_setup.py
   ```
   אמור לראות רק ✅ ירוקים

6. **הפעל**:
   ```bash
   python app.py
   # או
   ./run.sh
   ```

7. **פתח בדפדפן**:
   ```
   http://localhost:7860
   ```

### יתרונות:
- ✅ שימוש ללא הגבלה
- ✅ מהיר יותר
- ✅ עובד גם בלי אינטרנט (אחרי ההתקנה)
- ✅ שליטה מלאה

### חסרונות:
- ⚠️ צריך להתקין ffmpeg ו-Python
- ⚠️ לוקח 5-10 דקות להגדרה

---

## 🤔 איזו אפשרות לבחור?

### בחר ב-**Colab** אם:
- רוצה לבדוק מהר את המערכת
- לא בטוח אם תשתמש בזה באופן קבוע
- לא נוח לך להתקין דברים במחשב
- רוצה להראות למישהו איך זה עובד

### בחר ב-**מקומי** אם:
- תשתמש בזה באופן קבוע
- רוצה ביצועים מקסימליים
- יש לך Python ו-ffmpeg או נוח לך להתקין
- רוצה להתאים אישית את הקוד

---

## 💡 טיפ:
התחל עם **Colab** לבדיקה מהירה, ואם אהבת - עבור למקומי לשימוש קבוע!

---

## 🆘 צריך עזרה?

- **בדיקת מערכת**: הרץ `python test_setup.py`
- **בעיות**: ראה `INSTALL_INSTRUCTIONS.md`
- **תיעוד מלא**: ראה `README.md`
